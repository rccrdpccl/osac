{{- if .Values.echoAdapter.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "osac-metering.fullname" . }}-echo-adapter
  labels:
    {{- include "osac-metering.labels" . | nindent 4 }}
    app.kubernetes.io/component: echo-adapter
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "osac-metering.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: echo-adapter
  template:
    metadata:
      labels:
        {{- include "osac-metering.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: echo-adapter
    spec:
      serviceAccountName: {{ include "osac-metering.fullname" . }}
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      volumes:
        - name: kafka-secrets
          emptyDir:
            medium: Memory
            sizeLimit: 1Mi
        - name: ca-bundle
          configMap:
            name: {{ .Values.certs.caBundle.configMap }}
        - name: tmp
          emptyDir:
            sizeLimit: 10Mi
      initContainers:
        - name: fetch-kafka-secrets
          image: {{ .Values.cliImage | default "quay.io/openshift/origin-cli:4.20.0" }}
          env:
            - name: HOME
              value: /tmp
            - name: SOURCE_NS
              value: {{ include "osac-metering.kafkaClusterNamespace" . }}
            - name: SASL_SECRET
              value: osac-metering-echo-adapter
          volumeMounts:
            - name: kafka-secrets
              mountPath: /etc/kafka-secrets
            - name: tmp
              mountPath: /tmp
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 100m
              memory: 128Mi
          command:
            - /bin/bash
            - -euo
            - pipefail
            - -c
            - |
              DEADLINE=$((SECONDS + 300))
              wait_for_secret() {
                local name=$1 ns=$2
                echo "Waiting for secret ${name} in ${ns} (timeout 5m)..."
                until oc get secret "${name}" -n "${ns}" > /dev/null 2>&1; do
                  if [ $SECONDS -ge $DEADLINE ]; then
                    echo "Timed out waiting for secret ${name} in ${ns}"
                    exit 1
                  fi
                  sleep 5
                done
              }

              wait_for_secret "${SASL_SECRET}" "${SOURCE_NS}"

              oc get secret "${SASL_SECRET}" -n "${SOURCE_NS}" -o jsonpath='{.data.password}' | base64 -d > /etc/kafka-secrets/password
              echo "Kafka SASL secret fetched."
      containers:
        - name: echo-adapter
          image: "{{ .Values.echoAdapter.image.repository }}:{{ .Values.echoAdapter.image.tag }}"
          imagePullPolicy: {{ .Values.echoAdapter.image.pullPolicy }}
          env:
            - name: KAFKA_BROKERS
              value: {{ include "osac-metering.kafkaBrokers" . | quote }}
            - name: KAFKA_TLS_ENABLED
              value: "true"
            - name: KAFKA_TLS_CA_CERT
              value: /etc/ca-bundle/bundle.pem
            - name: KAFKA_SASL_USERNAME
              value: osac-metering-echo-adapter
            - name: KAFKA_CONSUMER_GROUP
              value: osac-metering-echo-adapter
            - name: KAFKA_SASL_PASSWORD_FILE
              value: /etc/kafka-secrets/password
            - name: DLQ_TOPIC
              value: "osac.metering.dlq"
            - name: DLQ_ENABLED
              value: "true"
            - name: METRICS_ADDR
              value: ":8080"
          volumeMounts:
            - name: kafka-secrets
              mountPath: /etc/kafka-secrets
              readOnly: true
            - name: ca-bundle
              mountPath: /etc/ca-bundle
              readOnly: true
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
{{- end }}
