//
// Copyright (c) 2025 Red Hat, Inc.
//
// Licensed under the Apache License, Version 2.0 (the "License"); you may not use this file except in compliance with
// the License. You may obtain a copy of the License at
//
//   http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software distributed under the License is distributed on
// an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied. See the License for the
// specific language governing permissions and limitations under the License.
//

syntax = "proto3";

package osac.public.v1;

import "google/api/annotations.proto";
import "osac/public/v1/add_on_operator_type.proto";

message AddOnOperatorsListRequest {
  // Index of the first result. If not specified the default value will be zero.
  optional int32 offset = 1;

  // Maximum number of results to be returned by the server. When not specified all the results will be returned. Note
  // that there may not be enough results to return, and that the server may decide, for performance reasons, to return
  // less results than requested.
  optional int32 limit = 2;

  // Filter criteria.
  //
  // The value of this parameter is a [CEL](https://cel.dev) expression used to select which objects to return. The
  // built-in `this` variable refers to the object being tested and `now` refers to the current date and time. If the
  // expression evaluates to `true` the object is included in the results. For example, to retrieve all add-on operators
  // with titles starting with `GPU`:
  //
  //     this.title.startsWith("GPU")
  //
  // If this isn't provided, or if the value is empty, then all the add-on operators that the user has permission to
  // see will be returned. Not all CEL constructs are currently supported for implementation reasons; see the filter
  // documentation (docs/FILTER.md) for the full details.
  optional string filter = 3;

  // Order criteria.
  //
  // The syntax of this parameter is similar to the syntax of the _order by_ clause of a SQL statement, but using the
  // names of the attributes of the add-on operator instead of the names of the columns of a table. For example, in
  // order to sort the add-on operators descending by title the value should be:
  //
  //     title desc
  //
  // If the parameter isn't provided, or if the value is empty, then the order of the results is undefined.
  optional string order = 4;
}

message AddOnOperatorsListResponse {
  // Actual number of items returned.
  int32 size = 1;

  // Total number of items of the collection that match the search criteria, regardless of the number of results
  // requested with the `limit` parameter.
  int32 total = 2;

  // List of results.
  repeated AddOnOperator items = 3;
}

message AddOnOperatorsGetRequest {
  string id = 1;
}

message AddOnOperatorsGetResponse {
  AddOnOperator object = 1;
}

service AddOnOperators {
  // Retrieves the list of add-on operators.
  rpc List(AddOnOperatorsListRequest) returns (AddOnOperatorsListResponse) {
    option (google.api.http) = {get: "/api/fulfillment/v1/add_on_operators"};
  }

  // Retrieves the details of one specific add-on operator.
  rpc Get(AddOnOperatorsGetRequest) returns (AddOnOperatorsGetResponse) {
    option (google.api.http) = {
      get: "/api/fulfillment/v1/add_on_operators/{id}"
      response_body: "object"
    };
  }

}
