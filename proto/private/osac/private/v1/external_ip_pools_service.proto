//
// Copyright (c) 2026 Red Hat, Inc.
//
// Licensed under the Apache License, Version 2.0 (the "License"); you may not use this file except in compliance with
// the License. You may obtain a copy of the License at
//
//   http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software distributed under the License is distributed on
// an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied. See the License for the
// specific language governing permissions and limitations under the License.
//

syntax = "proto3";

package osac.private.v1;

import "cleanapi/cleanapi.proto";
import "google/api/annotations.proto";
import "google/protobuf/field_mask.proto";
import "osac/private/v1/external_ip_pool_type.proto";

option (cleanapi.file).package = "osac.public.v1";
option (cleanapi.file).http_route_prefix_map = "private:fulfillment";

message ExternalIPPoolsListRequest {
  // Index of the first result. If not specified the default value will be zero.
  optional int32 offset = 1;

  // Maximum number of results to be returned by the server. When not specified all the results will be returned.
  optional int32 limit = 2;

  // Filter criteria.
  //
  // The value of this parameter is a [CEL](https://cel.dev) expression used to select which objects to return. The
  // built-in `this` variable refers to the object being tested and `now` refers to the current date and time. If the
  // expression evaluates to `true` the object is included in the results. For example, to retrieve all IPv4 external
  // IP pools:
  //
  //     this.spec.ip_family == IP_FAMILY_IPV4
  //
  // If this isn't provided, or if the value is empty, then all pools visible to the user are returned.
  optional string filter = 3;

  // Order criteria.
  //
  // The syntax of this parameter is similar to the syntax of the _order by_ clause of a SQL statement, but using the
  // names of the attributes of the external IP pool instead of the names of the columns of a table. For example, in
  // order to sort the pools descending by creation time the value should be:
  //
  //     metadata.created_at desc
  //
  // If the parameter isn't provided, or if the value is empty, then the order of the results is undefined.
  optional string order = 4 [(cleanapi.field).private = true];
}

message ExternalIPPoolsListResponse {
  // Actual number of items returned.
  int32 size = 1;

  // Total number of items matching the search criteria.
  int32 total = 2;

  // List of results.
  repeated ExternalIPPool items = 3;
}

message ExternalIPPoolsGetRequest {
  string id = 1;
}

message ExternalIPPoolsGetResponse {
  ExternalIPPool object = 1;
}

message ExternalIPPoolsCreateRequest {
  option (cleanapi.message).private = true;
  ExternalIPPool object = 1;
}

message ExternalIPPoolsCreateResponse {
  option (cleanapi.message).private = true;
  ExternalIPPool object = 1;
}

message ExternalIPPoolsUpdateRequest {
  option (cleanapi.message).private = true;
  ExternalIPPool object = 1;
  google.protobuf.FieldMask update_mask = 2;

  // Lock enables optimistic locking. When set to true, the server verifies that the current version of the object
  // matches the value of the metadata.version field of the submitted object. If they differ the update will be
  // rejected. This is useful to prevent lost updates when multiple clients are modifying the same object concurrently.
  bool lock = 3;
}

message ExternalIPPoolsUpdateResponse {
  option (cleanapi.message).private = true;
  ExternalIPPool object = 1;
}

message ExternalIPPoolsDeleteRequest {
  option (cleanapi.message).private = true;
  string id = 1;
}

message ExternalIPPoolsDeleteResponse {
  option (cleanapi.message).private = true;
}

message ExternalIPPoolsSignalRequest {
  option (cleanapi.message).private = true;
  string id = 1;
}

message ExternalIPPoolsSignalResponse {
  option (cleanapi.message).private = true;
}

service ExternalIPPools {
  // Retrieves the list of external IP pools visible to the caller.
  rpc List(ExternalIPPoolsListRequest) returns (ExternalIPPoolsListResponse) {
    option (google.api.http) = {get: "/api/private/v1/external_ip_pools"};
  }

  // Retrieves the details of one specific external IP pool.
  rpc Get(ExternalIPPoolsGetRequest) returns (ExternalIPPoolsGetResponse) {
    option (google.api.http) = {
      get: "/api/private/v1/external_ip_pools/{id}"
      response_body: "object"
    };
  }

  // Creates a new external IP pool.
  rpc Create(ExternalIPPoolsCreateRequest) returns (ExternalIPPoolsCreateResponse) {
    option (cleanapi.method).private = true;
    option (google.api.http) = {
      post: "/api/private/v1/external_ip_pools"
      body: "object"
      response_body: "object"
    };
  }
  // Updates an existing external IP pool. Only metadata fields (name, labels, annotations) are mutable.
  // All spec fields (cidrs, ip_family, implementation_strategy) are immutable after creation.
  rpc Update(ExternalIPPoolsUpdateRequest) returns (ExternalIPPoolsUpdateResponse) {
    option (cleanapi.method).private = true;
    option (google.api.http) = {
      patch: "/api/private/v1/external_ip_pools/{object.id}"
      body: "object"
      response_body: "object"
    };
  }
  // Deletes an external IP pool. Rejected if allocated ExternalIPs still reference this pool.
  rpc Delete(ExternalIPPoolsDeleteRequest) returns (ExternalIPPoolsDeleteResponse) {
    option (cleanapi.method).private = true;
    option (google.api.http) = {delete: "/api/private/v1/external_ip_pools/{id}"};
  }
  // Indicates that something changed in the object or the system that may require reconciling the object.
  rpc Signal(ExternalIPPoolsSignalRequest) returns (ExternalIPPoolsSignalResponse) {
    option (cleanapi.method).private = true;
  }
}
