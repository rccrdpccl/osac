//
// Copyright (c) 2026 Red Hat, Inc.
//
// Licensed under the Apache License, Version 2.0 (the "License"); you may not use this file except in compliance with
// the License. You may obtain a copy of the License at
//
//   http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software distributed under the License is distributed on
// an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied. See the License for the
// specific language governing permissions and limitations under the License.
//

syntax = "proto3";

package osac.private.v1;

import "cleanapi/cleanapi.proto";
import "google/api/annotations.proto";
import "google/protobuf/field_mask.proto";
import "osac/private/v1/external_ip_type.proto";

option (cleanapi.file).package = "osac.public.v1";
option (cleanapi.file).http_route_prefix_map = "private:fulfillment";

message ExternalIPsListRequest {
  // Index of the first result. If not specified the default value will be zero.
  optional int32 offset = 1;

  // Maximum number of results to be returned by the server. When not specified all the results will be returned. Note
  // that there may not be enough results to return, and that the server may decide, for performance reasons, to return
  // less results than requested.
  optional int32 limit = 2;

  // Filter criteria.
  //
  // The value of this parameter is a [CEL](https://cel.dev) expression used to select which objects to return. The
  // built-in `this` variable refers to the object being tested and `now` refers to the current date and time. If the
  // expression evaluates to `true` the object is included in the results. For example, to retrieve all external IPs
  // in ALLOCATED state:
  //
  //     this.status.state == EXTERNAL_IP_STATE_ALLOCATED
  //
  // If this isn't provided, or if the value is empty, then all the external IPs that the user has permission to see
  // will be returned. Not all CEL constructs are currently supported for implementation reasons; see the filter
  // documentation (docs/FILTER.md) for the full details.
  optional string filter = 3;

  // Order criteria.
  //
  // The syntax of this parameter is similar to the syntax of the _order by_ clause of a SQL statement, but using the
  // names of the attributes of the external IP instead of the names of the columns of a table. For example, in order
  // to sort the external IPs descending by creation time the value should be:
  //
  //     metadata.created_at desc
  //
  // If the parameter isn't provided, or if the value is empty, then the order of the results is undefined.
  optional string order = 4;
}

message ExternalIPsListResponse {
  // Actual number of items returned. Note that this may be smaller than the value requested in the `limit` parameter
  // of the request if there are not enough items, or if the system decides that returning that number of items isn't
  // feasible or convenient for performance reasons.
  int32 size = 1;

  // Total number of items of the collection that match the search criteria, regardless of the number of results
  // requested with the `limit` parameter.
  int32 total = 2;

  // List of results.
  repeated ExternalIP items = 3;
}

message ExternalIPsGetRequest {
  string id = 1;
}

message ExternalIPsGetResponse {
  ExternalIP object = 1;
}

message ExternalIPsCreateRequest {
  ExternalIP object = 1;
}

message ExternalIPsCreateResponse {
  ExternalIP object = 1;
}

message ExternalIPsUpdateRequest {
  ExternalIP object = 1;
  google.protobuf.FieldMask update_mask = 2;

  // Lock enables optimistic locking. When set to true, the server verifies that the current version of the object
  // matches the value of the metadata.version field of the submitted object. If they differ the update will be
  // rejected. This is useful to prevent lost updates when multiple clients are modifying the same object concurrently.
  bool lock = 3;
}

message ExternalIPsUpdateResponse {
  ExternalIP object = 1;
}

message ExternalIPsDeleteRequest {
  string id = 1;
}

message ExternalIPsDeleteResponse {}

message ExternalIPsSignalRequest {
  option (cleanapi.message).private = true;
  string id = 1;
}

message ExternalIPsSignalResponse {
  option (cleanapi.message).private = true;
}

service ExternalIPs {
  // Retrieves the list of external IPs.
  rpc List(ExternalIPsListRequest) returns (ExternalIPsListResponse) {
    option (google.api.http) = {get: "/api/private/v1/external_ips"};
  }

  // Retrieves the details of one specific external IP.
  rpc Get(ExternalIPsGetRequest) returns (ExternalIPsGetResponse) {
    option (google.api.http) = {
      get: "/api/private/v1/external_ips/{id}"
      response_body: "object"
    };
  }

  // Creates a new external IP. The spec.pool field determines which ExternalIPPool the address is allocated from.
  rpc Create(ExternalIPsCreateRequest) returns (ExternalIPsCreateResponse) {
    option (google.api.http) = {
      post: "/api/private/v1/external_ips"
      body: "object"
      response_body: "object"
    };
  }

  // Updates an existing external IP. Allows modifying metadata (labels, annotations). The spec.pool field is immutable.
  rpc Update(ExternalIPsUpdateRequest) returns (ExternalIPsUpdateResponse) {
    option (google.api.http) = {
      patch: "/api/private/v1/external_ips/{object.id}"
      body: "object"
      response_body: "object"
    };
  }

  // Deletes an external IP. The allocated address is returned to the parent pool's available capacity.
  rpc Delete(ExternalIPsDeleteRequest) returns (ExternalIPsDeleteResponse) {
    option (google.api.http) = {delete: "/api/private/v1/external_ips/{id}"};
  }

  // Indicates that something changed in the object or the system that may require reconciling the object.
  rpc Signal(ExternalIPsSignalRequest) returns (ExternalIPsSignalResponse) {
    option (cleanapi.method).private = true;
  }
}
