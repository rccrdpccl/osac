# OpenShift AI 4.20 Cluster catalog item
# An OpenShift 4.20 cluster pre-configured for AI workloads with RHOAI operator,
# GPU support, and Models-as-a-Service.

'@type': type.googleapis.com/osac.private.v1.ClusterCatalogItem
metadata:
  name: ocp-4-20-openshift-ai-cluster
title: OpenShift AI 4.20 Cluster
description: An OpenShift 4.20 cluster pre-configured for AI workloads. Includes Red Hat OpenShift AI (RHOAI) operator,
  GPU node support, and Models-as-a-Service. Ready-to-use for model serving, training, and inference.
template:
  id: osac.templates.ocp_4_20_openshift_ai
  shared: true
published: true
fields:
  network:
    pod_cidr:
      editable:
        default_value: 10.128.0.0/14
    service_cidr:
      editable:
        default_value: 172.30.0.0/16
