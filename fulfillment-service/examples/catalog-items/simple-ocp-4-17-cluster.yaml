# Simple OpenShift 4.17 Cluster catalog item
# A small OpenShift 4.17 cluster with 2 worker nodes on fc430 hardware.
# Suitable for development and testing workloads.

'@type': type.googleapis.com/osac.private.v1.ClusterCatalogItem
metadata:
  name: simple-ocp-4-17-cluster
title: Simple OpenShift 4.17 Cluster
description: A small OpenShift 4.17 cluster with 2 worker nodes on fc430 hardware. Suitable for development and
  testing workloads.
template:
  id: osac.templates.ocp_4_17_small
  shared: true
published: true
fields:
  network:
    pod_cidr:
      editable:
        default_value: 10.128.0.0/14
    service_cidr:
      editable:
        default_value: 172.30.0.0/16
